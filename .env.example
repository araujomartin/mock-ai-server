# Mock AI Server - Environment Template
# Copy this file to .env and fill in your values

# LLM provider to use (currently only 'openai' is supported)
LLM_PROVIDER=openai

# Your API key for the provider
# For OpenAI, use a key starting with 'sk-...'
LLM_API_KEY=your-openai-api-key

# Model to use with the provider
# Recommended: gpt-4o-mini (fast/cheap) or gpt-4o for higher quality
LLM_MODEL=gpt-4o-mini
